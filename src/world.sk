class World {
  const _scene dynamic
  const _terrain Terrain

  def new(scene dynamic) {
    _scene = scene
    _terrain = Terrain.new(self)
  }

  def scene dynamic {
    return _scene
  }

  def tick(eye dynamic) {
    _terrain.tick(eye)
  }
}

namespace World {
  const CLEAR_COLOR = THREE.Color.new(0x7FBFFF)
}
